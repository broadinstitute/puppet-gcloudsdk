---
version: 2
updates:
  # We use PDK, so don't auto-update Ruby dependencies
  # - package-ecosystem: bundler
  #   commit-message:
  #     prefix: fix(deps)
  #   directory: /
  #   groups:
  #     bundler:
  #       patterns:
  #         - '*'
  #   schedule:
  #     # Check for updates to Ruby gems every month
  #     interval: monthly
  - package-ecosystem: github-actions
    commit-message:
      prefix: fix(deps)
    directory: /
    groups:
      actions:
        patterns:
          - "*"
    schedule:
      # Check for updates to GitHub Actions every month
      interval: monthly
