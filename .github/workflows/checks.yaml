---
name: 'checks'

'on':
  pull_request:
    branches:
      - 'main'

jobs:
  pre-commit:
    uses: broadinstitute/shared-workflows/.github/workflows/pre-commit.yaml@tex_rust
  static-analysis:
    uses: broadinstitute/shared-workflows/.github/workflows/puppet-lint.yaml@tex_rust
  unit-tests:
    uses: broadinstitute/shared-workflows/.github/workflows/puppet-unit-test.yaml@tex_rust
